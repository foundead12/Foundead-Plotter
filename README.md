# Foundead Plotter

A simple Flask web app that plots mathematical functions.

## Features
- Enter a function (e.g. sin(x), cos(x)).
- Customize axis ranges and labels.
- Download the generated plot.

## How to Run
1. Clone the repo:
   ```bash
   git clone https://github.com/foundead12/Foundead-Plotter.git
